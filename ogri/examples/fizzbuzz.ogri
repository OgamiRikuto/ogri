define fizzbuzz using limit that
    number is 1.

    repeat until number is larger than limit that
        is_fizz is number modulo 3 is same as 0.
        is_buzz is number modulo 5 is same as 0.

        when is_fizz and is_buzz,
            say with "FizzBuzz".
        otherwise when is_fizz,
            say with "Fizz".
        otherwise when is_buzz,
            say with "Buzz".
        otherwise,
            say with number.
        
        number is number plus 1.

fizzbuzz with 20.
